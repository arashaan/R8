namespace R8.AspNetCore.Routing
{
    public static class PageRoutingExtensions
    {
        //public static string Page<TPage>(this TPage page, bool endpointRoute = false) where TPage : PageModel
        //{
        //    var currentConfig = new PageHandlerConfiguration
        //    {
        //        EndWithIndex = true
        //    };
        //    var address = PageHandlers.GetPagePath(typeof(TPage), currentConfig);
        //    return endpointRoute
        //        ? page.Page(address)
        //        : address;
        //}
    }
}